name: Publish to npm

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write  # required for npm OIDC trusted publishing

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - run: bun install
      - run: bun run build

      - name: Publish @ageflow/core
        run: npm publish --access public --provenance
        working-directory: packages/core

      - name: Publish @ageflow/executor
        run: npm publish --access public --provenance
        working-directory: packages/executor

      - name: Publish @ageflow/runner-claude
        run: npm publish --access public --provenance
        working-directory: packages/runners/claude

      - name: Publish @ageflow/runner-codex
        run: npm publish --access public --provenance
        working-directory: packages/runners/codex

      - name: Publish @ageflow/runner-api
        run: npm publish --access public --provenance
        working-directory: packages/runners/api

      - name: Publish @ageflow/testing
        run: npm publish --access public --provenance
        working-directory: packages/testing

      - name: Publish @ageflow/mcp-server
        run: npm publish --access public --provenance
        working-directory: packages/mcp-server

      - name: Publish @ageflow/server
        run: npm publish --access public --provenance
        working-directory: packages/server

      - name: Publish @ageflow/cli
        run: npm publish --access public --provenance
        working-directory: packages/cli
